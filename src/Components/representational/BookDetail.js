import React from "react";



const BookDetail = props => {
    console.log(props.book);

    if (props.book == null) return <div>

    </div>

    return (

        <div>

        </div>
    );
}

export default BookDetail;